services:
  cockpit:
    build: .
    ports:
      - "8420:8420"
    volumes:
      - ./graph-data:/data
    environment:
      - BRAIN_JSON_PATH=/data/graph.json
      - CORS_ORIGINS=*
    restart: unless-stopped
