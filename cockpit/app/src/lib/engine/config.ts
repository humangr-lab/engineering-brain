/** Engine configuration — matches original CONFIG from cockpit. */
export const ENGINE_CONFIG = {
  BLOOM_STRENGTH: 0.25,
  BLOOM_RADIUS: 0.3,
  BLOOM_THRESHOLD: 0.92,
  ORTHO_FRUSTUM: 14,
  INSTANCED_THRESHOLD: 100,
  DEFAULT_THEME: "dark" as const,
};
