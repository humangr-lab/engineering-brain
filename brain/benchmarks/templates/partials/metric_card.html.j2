<div class="metric-card">
  <div class="label">{{ label }}</div>
  <div class="value">{{ value }}</div>
  {% if delta is defined %}
  <div class="delta {{ 'positive' if delta_positive else 'negative' if delta_negative else 'neutral' }}">
    {{ delta }}
  </div>
  {% endif %}
</div>
